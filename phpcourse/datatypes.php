<?php

#integer
  $num = 4;

  var_dump($num);

#float

  $float = 2.25;

  var_dump($float);

#booleans

  $bool = true;

  var_dump($bool);

  $bool = false;

  var_dump($bool);

#strings

  $str = "hello from 11";

  var_dump($str);

#null

  $var = null;

  var_dump($var);

  $num = 5;
  
  $float = 2.8;
  
  echo $num + $float;

 /* $var = 'myVar', true;
  
  echo $var;*/

  # Exercise

  # assign two variables of type numbers the first name int and assign it to an integer value: 5 and the other name float and assign it to a float value: 2.8 number and add them using the addition sign  while your at it echo it out

  $num = 5;
  
  $float = 2.8;
  
  echo $num + $float;

  # Create a variable of type string name it myString and assign it to value of "this is a simple string" and echo it out

  $myString = "this is a simple string";
  
  echo $myString;

  # assign a variable name it "myVar" to value of  "true" and echo it out

  $myVar = true;
  
  echo $myVar;

  # create a variable name it nothing and assign it to a value of null and print it out using print

  $nothing = NULL;

  print $nothing;